/**
 *  FUNCTIONS
 * 
 * the two phases to using functions: first we must ?. next we can execute a function (two more words for executing a function) by?
 * diff between parameters and args passed to function
 * syntax for a named function?
 * how to assign function to a variable?
 * how to specify inputs and outputs when function can take in both optionally?
 * explain function scopes (parent and child relationship etc)
 * closures
 * 
 * 
 * 
 * 
 */